extends base.pug

block content 
    video(src="/" + video.fileUrl, controls, height="500")
    div
        p=video.description
        small=video.createdAt
    a(href=`${video.id}/edit`) Edit Video &rarr;
    br
    a(href=`${video.id}/delete`) Delete Video &rarr; 
    